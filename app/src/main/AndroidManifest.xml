<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymMembership"
        tools:targetApi="31">
        <!-- User Start -->
        <activity
            android:name=".User.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".User.Signup"
            android:exported="false" />
        <activity
            android:name=".User.Home"
            android:exported="false" />
        <activity
            android:name=".User.MembershipPurchase"
            android:exported="false" />
        <activity
            android:name=".User.PaymentProcess"
            android:exported="false"/>
        <activity
            android:name=".User.ChooseTrainer"
            android:exported="false" />
        <activity
            android:name=".User.BMIProcess"
            android:exported="false" />
        <!-- User Ends -->
        <!-- Admin Start -->
        <activity android:name=".Admin.Login"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Admin.Home"
            android:exported="false" />

        <!-- Admin Ends -->
    </application>

</manifest>